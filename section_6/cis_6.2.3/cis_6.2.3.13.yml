---

{{ if .Vars.ubtu24cis_level_2 }}
  {{ if .Vars.ubtu24cis_rule_6_2_3_13 }}
command:
  auditd_14_config:
    title: 6.2.3.13 | Ensure events that modify /etc/shadow and /etc/gshadow are collected | Config
    exec: grep identity /etc/audit/rules.d/*.rules
    exit-status: 0
    stdout:
    - '-w /etc/gshadow -p wa -k identity'
    - '-w /etc/shadow -p wa -k identity'
    meta:
      server: 2
      workstation: 2
      CIS_ID:
      - 6.2.3.13
      NIST800-53R5:
      - AU-3
  {{ end }}
{{ end }}
