---

{{ if .Vars.ubtu24cis_level_1 }}
  {{ if .Vars.ubtu24cis_rule_6_1_1_4 }}
service:
  {{ if eq .Vars.ubtu24cis_syslog_service "journald" }}
  single_syslog_journald_running:
    title: 6.1.1.4 | Ensure only one logging system is in use
    name: systemd-jounald.service
    running: true
    enabled: true
    meta:
      server: 1
      workstation: 1
      CIS_ID:
      - 6.1.1.4
  single_syslog_rsyslod_stopped:
    title: 6.1.1.4 | Ensure only one logEnsure only one logging system is in useservice
    running: false
    enabled: false
    meta:
      server: 1
      workstation: 1
      CIS_ID:
      - 6.1.1.4
  {{ end }}
  {{ if eq .Vars.ubtu24cis_syslog_service "rsyslog" }}
  single_syslog_rsyslog_running:
    title: 6.1.1.4 | Ensure only one logging system is in uEnsure only one logging system is in use: true
    enabled: true
    meta:
      server: 1
      workstation: 1
      CIS_ID:
      - 6.1.1.4
  single_syslog_journald_stopped:
    title: 6.1.1.4 | Ensure only one logging system is in use | journald stoppEnsure only one logging system is in use enabled: false
    meta:
      server: 1
      workstation: 1
      CIS_ID:
      - 6.1.1.4
    {{ end }}
  {{ end }}
{{ end }}
